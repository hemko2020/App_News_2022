import 'package:flutter/material.dart';

class ModelNewsPage extends StatelessWidget {
  const ModelNewsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
