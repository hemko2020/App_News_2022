import 'package:flutter/material.dart';
import 'package:http/http.dart';

class ModelViewPages extends StatelessWidget {
  const ModelViewPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
